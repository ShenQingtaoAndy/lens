spring:
  profiles:
    active: product
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/photo
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true